data:
  class_path: src.data.WireframeDataModule
  init_args:
    # ----- paths (relative to project root) -----
    data_root: data
    # RF branch trains on the RAW (uncleaned) wireframes -- the arc-length RF
    # target tolerates dense / messy edges, so no cleaning step is needed.
    train_edge_subdir: train/sample_edge
    train_pointcloud_subdir: train/sample_pointcloud
    predict_pointcloud_subdir: test/sample_pointcloud
    # Split over the raw edge dir; auto-built on first run.
    split_path: data/split.json
    recursive_glob: false

    # ----- train/val split (test/ has no GT edges, so val is carved from train) -----
    # Build once with: python scripts/make_split.py
    # use_val: true  -> train on saved "train" list, validate on "val".
    # use_val: false -> train on ALL data (train + val), no validation loader.
    use_val: true
    auto_build_split: true
    train_ratio: 0.9
    split_seed: 42

    # ----- graph / point-cloud / RF-target format -----
    vertex_merge_tol: 0.0001
    # Caps disabled (0): RF trains on the full raw distribution, no oversize
    # skipping (the fixed-size arc-length target absorbs dense samples).
    max_vertices: 0
    max_edges: 0
    num_edge_points: 32
    pc_num_points: 4096
    # RF target point budget N: every sample yields wf_points (8192, 4).
    wf_num_points: 8192
    min_edges: 1
    max_load_retries: 64

    # ----- data loader -----
    shuffle: true
    num_workers: 8
    batch_size: 16
    pin_memory: true
    prefetch_factor: 2
    persistent_workers: true
